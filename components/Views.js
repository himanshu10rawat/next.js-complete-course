// import { cookies } from "next/headers";

export default async function Views() {
  // cookies();
  // await new Promise((resolve) => setTimeout(resolve, 5000));
  return <h2>This is views page</h2>;
}
